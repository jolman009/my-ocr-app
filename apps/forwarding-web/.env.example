# Receipt Radar API — used ONLY for login (shared JWT auth across both products).
VITE_AUTH_API_URL=http://localhost:4000/api

# Manifest 956 forwarding API — all document/customer/org data.
VITE_FORWARDING_API_URL=http://localhost:4001/forwarding
