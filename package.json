{
  "name": "receipt-ocr-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/mobile",
    "packages/shared"
  ],
  "overrides": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "scripts": {
    "dev:api": "npm run dev --workspace api",
    "dev:web": "npm run dev --workspace web",
    "dev:mobile": "npm run start --workspace mobile",
    "build": "npm run build --workspace @receipt-ocr/shared && npm run build --workspace api && npm run build --workspace web",
    "test": "npm run test --workspace api && npm run test --workspace web",
    "lint": "npm run lint --workspace @receipt-ocr/shared && npm run lint --workspace api && npm run lint --workspace web && npm run lint --workspace mobile"
  }
}
